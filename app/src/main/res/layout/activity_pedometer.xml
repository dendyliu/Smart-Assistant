<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_pedometer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="org.kdadev.smartassistant.PedometerActivity">

    <ImageView
        android:id="@+id/steplogo"
        android:layout_width="match_parent"
        android:layout_height="210dp"
        android:src="@drawable/pedometer"/>
    <TextView
        android:layout_below="@id/steplogo"
        android:id="@+id/stepcount"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        tools:text="120 Steps"
        android:text="0 Step"
        android:textSize="60sp"/>
    <TextView
        android:id="@+id/steptime"
        android:layout_below="@id/stepcount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="30sp"
        tools:text="since 18:25"/>
    <Button
        android:id="@+id/stepbutton"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/steptime"
        android:layout_width="150dp"
        android:layout_height="80dp"
        android:text="START"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:textAppearance="@style/TextAppearance.AppCompat"
        style="@style/Widget.AppCompat.Button"
        android:background="@color/colorPrimary"
        android:backgroundTint="@color/colorAccent"
        android:elevation="9dp" />

</RelativeLayout>
